# ── .NET build / publish ─────────────────────────────────────
**/bin/
**/obj/
dist/
publish/

# ── Compiled / binary ────────────────────────────────────────
*.o
*.exe
*.dll
*.pdb

# ── WiX installer outputs ────────────────────────────────────
*.msi
*.wixpdb
*.wixobj
*.cab

# ── NuGet ────────────────────────────────────────────────────
*.nupkg
.nuget/

# ── Temporary files ──────────────────────────────────────────
*.tmp
*.bak

# ── IDE / editor ─────────────────────────────────────────────
# .vscode/ is tracked — workspace settings are part of the project
.idea/
*.swp
*.swo
*.pui
*.prj
*~
*.user
*.suo

# ── Logs generated by RegiLattice ────────────────────────────
*.log

# ── OS cruft ─────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store

# ── Test / coverage artifacts ────────────────────────────────
htmlcov/
**/TestResults/
**/coverage.cobertura.xml
**/diag*.txt
# ── Mutation testing (Stryker.NET) ──────────────────────────────────────────
**/StrykerOutput/
mutation-report*/

# ── MCP memory server knowledge graph (auto-generated, not source-controlled) ──
.github/mcp-memory.jsonl

# ── Benchmark results ────────────────────────────────────────────────────────
BenchmarkDotNet.Artifacts/
# ── Profiling data ───────────────────────────────────────────
*.dat
*.nettrace
*.etl

# ── Archive (legacy Python + old NativeGUI — no longer tracked) ─
archive/

# ── Python leftovers ─────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/

# ── Generated files ──────────────────────────────────────────
current-ids.txt

# ── Development temporary / intermediate files ──────────────
.tmp/
build_output.txt

.fake

# ── Local scratch / planning notes (not tracked) ─────────────
C0 - FE HBI changes.txt
Think deep and plan a full refactor.txt
